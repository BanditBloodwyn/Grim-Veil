namespace Globals;

public static class Settings
{
    // Debug
    public static bool SHOWDEBUGINFO = true;

    // Camera
    public static bool ALLOW_CAMERA_ROTATING = false;

    // Behaviour
    public static float MINIMUM_SPLASHSCREEN_TIME_SECONDS = 2f;

    // Layout
    public static int SPLASHSCREEN_WIDTH = 900;
    public static int SPLASHSCREEN_HEIGHT = 561;
    public static int LOGO_SIZE_DEVIDER = 9;

    // World
    public static int DEFAULT_TILE_SIZE = 50;
}
